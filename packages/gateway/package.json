{
  "name": "@axis/gateway",
  "version": "1.0.0",
  "description": "AXIS AI API Gateway — auth, cross-channel rate limiting, nonce enforcement, DDoS protection and audit logging in front of the verification engine.",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node src/db.js --migrate"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.1",
    "fastify": "^4.27.0",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0",
    "winston": "^3.13.0"
  }
}
